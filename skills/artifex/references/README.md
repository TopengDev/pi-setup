# /artifex reference library

Curated montages from the ref-study of 12 award-caliber sites (real fonts read from each DOM, captured headless). These ship with the skill so `/artifex` designs **grounded in studied technique, not the model's idea of "engaging" (N8)**. Study the montage for the technique you're about to build before you build it.

The six montages bundled here are the most instructive + reasonably sized of the 12 studied award sites. They are self-contained — no external ref-study directory is needed to build from them.

| File | Site | What to study it for | Technique(s) | Tier |
|---|---|---|---|---|
| `crescente.png` ★ | Sicilian street food | **Best buildable F&B blueprint.** Flat color-field swaps (orange↔cream), rounded-panel reveals, inline-icon headlines, curved sticker-type, one 3D-product moment | T1, T2, T3, T5, T11 | B-heavy |
| `chungiyoo.png` ★ | designer portfolio | **Type-as-hero, cheapest variance family.** Color-field swaps (cream/yellow/pink) + serif/script/outline type variety + floating & tilted cards + arc-text. Mostly CSS/type | T3, T4, T5, T9 | B-heavy |
| `lusion.png` | creative-dev studio | **The canonical variance signature** — physics-hero → color-cut → editorial-grid → gallery → kinetic-type+3D-ribbon → framed-3D. Six skeletons, zero repeat. Aeonik + Plex Mono eyebrows | T1, T3, T4, T7, T14, T15, T18 | mixed (1 H/E hero) |
| `synchronized.png` | digital studio | **Index-numbered case panels on muted color-fields** + circular type-rings retired after one use. The "design-studio editorial" template | T5, T8 | B |
| `mana.png` | yerba-mate brand | **Product-anchored card swap** — product pinned while benefit cards swap around it. Flat-illustration collage + 3D product. Warm F&B | T21, T22 | B |
| `wix-pantone.png` | Pantone CotY capsule | **Bento texture collage** + editorial hero + gradient band, all in ONE monochrome. Sans×classical-serif contrast | T3, T7 | B |

**Not bundled here, and why:**
- `pioneer-resn` — **Elite tier (T19 continuous 3D world morph).** A named *warning*, not a target — do not attempt.
- `prometheus` — cautionary: a once-legendary WebGL site regressed to generic Elementor + Plus Jakarta Sans. Mood ref only.
- `noomo-xr` — synthwave grid + mono knockout (T20/T6), but the heaviest montage; capture a fresh reference if building retro-tech.
- `kpr` — cinematic game-trailer pacing (T12/T13/T14); pull from notes if building a worldbuilding/web3 brief.
- `awwwards` — the polished baseline (card grid + hover-video, T16), not a wow ref.
- `mammut` — site was down at capture; horizontal mountain-ascent storytelling (T10) as a named precedent only.

**Fastest paths by brief:** warm consumer / F&B → `crescente` + `mana`. Editorial / portfolio / type-led → `chungiyoo` + `synchronized` + `wix-pantone`. Tech / data / dev → `lusion` (+ `noomo-xr`, `kpr` — not bundled; capture fresh if needed). Always cross-check the Variance Map (SKILL §6) against ≥ 2 refs.
